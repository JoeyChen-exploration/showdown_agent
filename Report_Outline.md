# Report 素材对照（对齐 AssessmentDescription.md）

> 这份文档不是报告本身，是把已经记录的 log 按 [AssessmentDescription.md](AssessmentDescription.md)
> 要求的三个部分（Design / Evaluation / Reflections）分类整理，标出"已经有什么素材"和"还缺什么"，
> 真正动笔写报告的时候直接从这里找对应的文件/记录，不用重新翻聊天记录回忆。
> 实际报告草稿（英文，正在写）在 [Report_Draft.md](Report_Daft.md)。

## 写作规范：代码引用必须自解释（2026-07-27）

report 本身不包含代码，所以正文里任何反引号包起来的函数名/变量名（`_effective_speed`、
`SETUP_BOOST_WEIGHT` 这类），第一次出现时旁边必须有大白话说明它是干什么的，不能假设
读者会去翻源码——要么直接在同一句话里用中文/英文讲清楚它计算/代表什么，要么放进有"含义"列的
定义表格（2.4 的参数表就是这个模式）。检查标准：**把反引号和函数名整个删掉，这句话是不是还
读得通、意思有没有丢**——读得通就说明这处引用只是"顺手点名证据来源"，没问题；读不通就说明
这处引用在替代本该写出来的解释，必须补。已经按这条标准把 Design/Evaluation/Reflections 全篇
过了一遍（issue #21），以后新写的内容（4.2/4.3/4.6/4.7 等还没写的 stub）同样要过这道检查。

## 篇幅分配提醒

| 部分 | 建议篇幅 | 目前素材成熟度 |
|---|---|---|
| Introduction | 未单独占分，但 Design 的论证要靠它铺垫 | 已有明确思路，见下 |
| Design | 2 页 | 较充分——`bots_strategy.md` 整个就是设计推理过程 |
| Evaluation | 2–3 页 | 有基础数据，但"证明机制按设计运作"这块还缺细粒度验证 |
| Reflections | 1–2 页 | 目前几乎没有专门素材，需要从现在开始边做边记 |

> 项目 8 月底才截止，现在时间充裕，所以这份大纲写得比"够用"更详细一些——目的是过几周回来接着做的时候，
> 看这一份就能想起来当时具体是怎么想的，不用重新复盘。

---

## 报告大纲（Table of Contents 草案）

下面是实际报告的章节结构草案，每条后面标了大概对应哪份素材文档。写的时候从这里对着列表逐条填，
不用从头构思结构。

**1. Introduction**（不单独占分，约 0.3–0.5 页）
- 1.1 团队背景声明：没有 Pokémon 对战经验，但有类似的卡牌对战经验
- 1.2 任务定义：Gen9 Ubers、15 个固定难度 bot 梯队、专家系统（非 ML）约束
- 1.3 调研阶段识别的关键 factor 列表：属性克制关系、技能特殊效果、特殊道具、太晶化、
  分级强度体系、teampreview 随机性、速度/出手顺序（**2026-07-24 从六条补成七条**——速度是
  `_effective_speed`/`_we_move_first` 贯穿整个决策架构的真实运行时因素，之前漏列了；见下面
  "Introduction 部分素材"）

**2. Design**（2 页，**2026-07-27 大重构**：用户指出正文太杂、混进了不该展示的过程叙事
（比如 90 秒超时实测心路历程），跟页数预算冲突——正文当时估算约为 6 页预算的 2.4 倍，Design
占最大头（约 46%）。重构后 2.1-2.8 八节合并成 2.1-2.4 四节，Design 部分的字数砍掉约 66%。
**局限性讨论整段移出 Design，改成完全放 Reflections 讨论**（用户明确要求，之前 2.8 那条待迁移
的内容——"权重是启发式初始值非理论推导"、"没有清钉手段"——目前还没正式转移过去，是接下来
Reflections 那边的一个具体待办，不要漏掉）。三处旧内容彻底删掉，不迁移：90 秒超时实测分析、
三场景评测对比表（`expert_main.py`/`expert_main_isolated.py`/`expert_competition.py`）、
"v1 不实现太晶化"的独立讨论（现在 v3 已实现，纯属历史，Reflections 4.1 已经讲过，Design 里
留着反而说不通）新结构：）
- 2.1 项目机制分析：只留两条——5 档队伍强度只约束对手不约束我方、poke_env 暴露的环境状态
  决定方法论下限
- 2.2 对手分析（**取代原 2.3+2.4**）：方法论一句话带过（Teambuilder 逐队解析 15 个 bot），
  主体是"威胁 → 具体表现"**两列**表格（**2026-07-27 二次调整**：应对内容拆出去了，不放在
  这张表里，见下面 2.3/2.4；表格结尾一句话指路"麻痹/先制招式的应对见 2.4，Choice 锁招/撒钉子/
  Knock Off 的应对见 2.3"）
- 2.3 队伍构成（**取代原 2.2**）：pokeaimmd.com/ubers 建队工具、Hyper Offense 打法定位一段话
  讲清楚（速度快+高伤害更好分配权重/计算，跟 2.4 的 greedy 打分架构直接呼应）+ 队伍构成表 +
  **2026-07-27 新增**：队伍构筑层面对 2.2 三类威胁的取舍（Choice 锁招无运行时应对+纯属巧合、
  撒钉子无应对、Knock Off 靠三只专属/消耗道具规避）；原来的两点 OTK 论证（时间跨度匹配、
  容错率更高）压缩进第一段话里，没有展开成独立小节
- 2.4 核心算法：if-else（开局）+ greedy（通用打分）（**取代原 2.5+2.6+2.7**）：决策级联图
  从最前面挪到这里；新增"最终 ablation 后核心参数"表（硬短路 KO/开局脚本/`SETUP_BOOST_WEIGHT`/
  `SWITCH_DEFENSE_WEIGHT`/`SWITCH_COST`/先手容忍阈值，每条都标消融结论，直接对应 Evaluation 3.3
  的数据，不重复展开）；"为什么 if-else"（开局 combo 消融证实有效）+ "为什么 greedy"（换人判断
  是连续数值权衡、不做多回合搜索的 AIMA 引用 [9]、对手出场随机+未来竞赛场景只知道已亮招式）
  合并成一段连贯论证，不再拆成"决策架构"/"具体机制"/"明确不做"三个独立小节

**3. Evaluation**（2–3 页）
- 3.1 评测方法说明：本地 15-bot 梯队、`n_challenges=3`、胜率 >0.5 记胜、排名→分数换算规则
- 3.2 迭代对比：满编队伍+随机（1/15，只赢 `random-ru`）vs. v1 规则系统上线（引用 3.3 消融表格的
  `baseline` 行，14.33/15，不重复列数据），量化"队伍强度"和"决策逻辑"各自的贡献（对应
  `Experiment_Log.md` 前两条记录）。**2026-07-22 决定**：占位基线（随机+1只精灵，0/15）不放进
  Evaluation——起点定在"满编队伍+随机"就够说明问题，占位基线只是开发早期的冒烟测试，不是有意义的
  量化对比点，放进去反而占页面又没有信息增量（6 页篇幅紧张，Evaluation 建议只有 2–3 页）。
- 3.3 消融实验：硬短路 KO / 开局脚本开关，铺垫权重/换人权重/`SWITCH_COST` 扫描
  （对应 `analysis/results/summary.csv`，证明"系统按设计运作"而不只是整体排名好看）
- 3.4 机制级验证（还没写）：`decision_log` 复盘具体案例（比如已经抓到的对手速度估算
  错误那个案例）、`simple-ou`/`simple-uu` 输局分析（呼应 issue #6）

（原 3.4"噪声来源分析"**2026-07-22 挪到 Reflections 4.4**（大纲编号 2026-07-27 因插入新 4.1 顺延）了——讲的是评测方法论本身的局限性，
更贴合 Reflections 的定位，也给这里腾页面。）

**4. Reflections**（1–2 页，**2026-07-27 在开头插入新的 4.1，原 4.1-4.6 依次顺延成 4.2-4.7**，
**同一天已正式转正写进 `Report_Draft.md`**：新 4.1 全文写完，原有的 4.3/4.4 也已经跟着改成
4.4/4.5，全文交叉引用一并核对过。4.2/4.3/4.6/4.7 仍是待写的 stub）

- **4.1 这次作业到底难在哪：从任务本身，到每个版本具体踩的坑（新增，呼应
  `COMPSYS726_Assignments.pdf` 里"不是让你抱怨难，是让你在报告里讲清楚为什么难"这条明确要求）**——
  全篇要往"专家系统"这条线上靠：专家系统的知识库是人写的，人会犯错，这跟 ML 系统"数据不够/
  过拟合"是完全不同性质的脆弱点（呼应 `Report_Draft.md` 顶部已经加的"醒目提醒"那段）。分两层讲：

  **任务本身固有的难点（不是实现难度，是这类问题本身的难度）**：
  1. 对手信息永远不完整——团队预览之外，具体数值/努力值/太晶属性都要等亮相才知道，逼着
     知识库里必须写"保守假设"类规则（`_max_speed_estimate`），而不是精确计算——**这正是专家
     系统在不完全信息环境下的典型形态：知识库里必须显式编码"不知道的时候怎么办"这条元规则**。
  2. 评测环境本身非确定——战斗机制随机性 + 对手随机首发，导致"这个改动到底有没有用"本身就
     很难回答，逼着我们建了一整套"多次重跑 confirm"的方法论（3.1 节）。
  3. 知识库的正确性完全压在人身上——没有训练数据能帮忙纠错，任何一次对游戏机制的理解偏差
     会直接编码进规则、且很难自己发现（好几个 bug 是全文件系统性审查才挖出来的，不是靠报错
     发现的）——**这是专家系统范式特有的脆弱点：ML 系统错在"学歪了"，专家系统错在"人想歪了
     还写死了"，报告里这个对比可以直接呼应课件"assignments are NOT hard to implement
     technically...think about what's making you frustrated"这条**。

  **每个版本具体踩的坑（都已经记在 `Experiment_Log.md` 里，直接摘）**：
  - **v1**：`_switch_score` 换人打分方向反了——不是笔误，是最初设计换人评分公式时，"进攻端"
    和"防御端"两个方向的语义本身理解错了，靠对局输赢异常倒推查出来的。属于"知识转译成代码"
    这一步出错的典型案例。
  - **v2**：`max_damage-uber` 拖到超时最初被诊断成"对面冥想回血叠盾拖时间"，围绕这个假设做了
    两版功能（Taunt 反叠盾、克制对照表），后来查 `bots/max_damage.py` 源码才发现假设本身是
    错的——这个 bot 永远只选最高威力招式，根本不会用变化招式。教训：**设计修复之前要先去读
    对手的实际实现，不能凭"典型打法"的经验去猜**，这也是专家系统"知识必须来自可验证的来源，
    不是直觉"这条原则的一次真实代价。
  - **v3（太晶化）**：code review 抓到两个隐藏 bug——Judgment 太晶后属性判断方向写反了（查
    poke_env 自带的官方伤害计算器源码才确认真相），以及 `mon.tera_type` 这个属性的真实语义
    （"协议已揭示"而不是"配置好的"）跟直觉完全相反——属于"框架 API 名字容易让人望文生义猜错"
    的坑，知识库里一条看似正确的规则，源头其实是对第三方工具语义的误解。
  - **消融工具本身的坑（issue #14）**：一度以为的"结构性、决策逻辑解决不了"的超时问题，最后
    查出来是我们自己的测试脚本有 bug（同一进程多次 repeat 用户名递增导致连接残留），不是被测
    的智能体本身。这是一类完全不同的困难——**不是"知识库写错了"，是"验证知识库对不对的工具
    本身不可靠"**，专家系统的解释设施再完整，也没法弥补测量装置本身的偏差。
  - **`_survivability_factor`/`SETUP_BOOST_WEIGHT`（issue #7）**：第一版修复方案逻辑上说得通，
    但 code review 指出它在"最常触发的场景"下完全不生效——一条推理链条听起来合理、却没有真正
    命中根因。改对之后又发现一个连带参数（`SETUP_BOOST_WEIGHT`）之前一直"因为乘在一个几乎恒
    为 0 的门槛上而没有意义"——一个 bug 会掩盖另一个参数的真实效果，修复的依赖关系比想象的复杂。
  - **招式先制修复（issue #15/#16）**：逻辑严格验证是对的（单测、code review 都过了），但消融
    测出一个真实、可复现、却查不清楚具体因果的小代价。**2026-07-27 专门查过"能不能固定随机种子
    重放"这条路是不是真的走不通**——不是直觉判断，是读了 `pokemon-showdown`（`sim/battle.ts`/
    `server/room-battle.ts`）和 `poke_env` 源码后确认的：底层引擎本身支持指定种子，但 `poke_env`
    实际使用的标准对战创建协议从来不会把种子暴露给客户端；更深一层的问题是，就算种子暴露出来，
    "从头重放、只改一回合决策"本身也不构成严格对照——决策一旦分叉，那一回合起的随机数消耗顺序
    本来就该不一样，要做真正干净的对照需要战斗中途存档/分支重跑（Python 决策逻辑 + Node 战斗
    引擎跨语言桥接），是一个跟这一行代码的验证任务完全不成比例的独立工程项目。教训：**不是所有
    因果都能被完全查清楚，有时候只能确认"相关性稳定存在"就要做取舍决策，这是非确定性环境本身的
    认识论极限，不是调查不够努力——而且这次连"这个局限本身有多深"都专门查证过，不是停留在
    "感觉查不清楚"这个直觉层面**。详见 `Experiment_Log.md` 2026-07-27 "issue #16 后续调查"。

  **串起来看**：难点的性质在演变——v1 是"知识转译成代码时出错"，v2 是"假设没验证就动手设计"，
  v3 之后开始出现"第三方框架语义比想象的复杂"和"测量工具本身不可靠"这两类元问题，最新阶段是
  "某些因果关系在非确定性环境下根本没法被完全查清楚"。这个演变过程本身就是"对专家智能体这个
  概念的新理解"最好的素材，也是 `COMPSYS726_Assignments.pdf` 明确要求的"reflect on how your
  method and understanding...have changed over the course"最直接的回应。

- **待办（2026-07-27 新增）**：原 Design 2.8 局限性讨论整段移出后，有两条内容目前在全文没有
  任何位置提起，还没找到新家——①打分公式里的权重是启发式给的初始值、不是理论推导出来的；
  ②队伍没有任何清钉/免疫钉子手段（没人带 Heavy-Duty Boots）。这两条不属于"没写"，是"从 Design
  搬走了但还没正式搬进 Reflections 哪一节"，下次写 Reflections 时要记得处理，不要漏掉
- 4.2 预期 vs 实际：队伍强度实验揭示的"纯规则系统面对会博弈的 `simple` AI 明显更吃力"现象
- 4.3 具体案例复盘：对手速度估算 bug，讨论"用有限信息（种族值）近似未知量（对手真实努力值）"
  这个专家系统里常见的权衡
- 4.4 评测本身的随机性（**2026-07-22 从 Evaluation 3.4 挪过来，2026-07-24 更新**）：三层原因——
  战斗机制本身的随机性（含对手随机首发，样本量再大也不会消失）→ 理论上 90 秒超时会把它折叠成
  少数对局的二元胜负标签 → 样本量太小导致这个二元结果可能直接误导单次判断。原来放在 Evaluation
  里偏"方法论说明"，实际讨论的是"设计正确≠单次排名一定好看"这个专家系统在随机环境下的根本局限，
  更贴合 Reflections 要求的"开发专家系统本身的挑战和局限性"，也给 Evaluation 腾出页面放硬数据
- 4.5 一次被推翻的"结构性问题"结论（**2026-07-24 新写完草稿**）：`max_damage-uber` 超时曾被
  当作决策范式解决不了的结构性缺陷，七八轮完全不同的代码改动下"稳定复现"，结果被证明是我们自己
  消融测试工具的偏差（同一进程多次 repeat 的连接残留），不是被测系统的属性——核心教训是"多次
  重跑结果稳定"只能排除随机噪声，排除不了"测量工具本身有偏差"这另一类错误
- 4.6 greedy 打分 vs if-elif/hardcode 的回顾性对比（**2026-07-27 写完**）：不需要假设，我们自己
  系统里两种写法本来就同时在跑（2.4 开局脚本=hardcode，其余=greedy）；用 3.3 节真实消融数据
  说话——`no_hard_ko`/`no_opening_script` 是仅有的两处可复现真实分差，权重类参数改动大多不敏感，
  反直觉地是两条 hardcode 规则贡献最大。接 [9] AIMA 分类框架收束成"按子问题的确定性程度选写法，
  不是预设通用框架更优越"，收尾点出两者本质上仍是同一类系统（都会犯知识编码错误），避免变成
  "hardcode 完胜 greedy"这种以偏概全的结论
- 4.7 对下一阶段（班级竞赛等）的展望（**待写**；太晶逻辑已在 v3 完成，见
  `Experiment_Log.md`，这里改成回顾而非展望）

**References**（不计入 6 页页数限制，**2026-07-27 已扩到 10 条**）：Expert system 定义
（Wikipedia）、`poke_env`、Pokemon Showdown 模拟器、Teambuilder、Smogon Gen9 Ubers 格式规则、
`pokeaimmd.com` 队伍随机化工具、课程 `showdown_agent` 起始仓库、**新增 3 条真实 peer-reviewed/
教材级文献**——Buchanan & Shortliffe (1984) MYCIN 专家系统原始文献 [8]、Russell & Norvig AIMA
task environment properties 分类 [9]、Mytkowicz et al. (2009) ASPLOS 系统评测方法论论文 [10]。
三篇都用 WebSearch 核实过真实存在，且都跟 Assignment 1 的专家系统/规则系统定位一致——**明确
排除了 ML/RL 相关文献**（用户提醒过 Assignment 2 才是 RL，原计划里的 Henderson et al. 2018 RL
论文已替换成 Mytkowicz et al.），每篇都在正文对应论证句子旁插了引用标记，不是只堆在列表里。
见 Report_Draft.md 末尾，随写作过程增补。

---

## Introduction 部分素材（不单独占分，但是 Design 论证的起点）

AssessmentDescription.md 没有把 Introduction 单独列为评分项，但报告不可能没有背景交代，
而且**这段背景交代本身就是 Design 部分"你是怎么理解这个问题的"这一论证链条的起点**，
所以先把这部分想清楚。

**开头的诚实定位声明**：团队在动手之前没有 Pokémon 对战经验，但有类似的卡牌对战经验
（策略层面的资源管理/克制关系/先手后手博弈是相通的）——这个声明的作用不是自谦，
而是给后面"为什么我们要先做一轮系统性调研才动手设计"这个方法论选择做铺垫：
正因为没有游戏本身的直觉积累，所以设计阶段必须**先把隐性知识显性化**（对应课程材料里
"专家系统的知识来自哪里"这个主题——我们的知识来源是"调研 + 文档化"，不是"个人经验直觉"）。

**调研阶段识别出的关键 factor**（建议在 Introduction 里逐条列出，每条配一句一句话解释）：

1. **属性克制关系（type effectiveness）**——18 种属性两两之间的伤害倍率关系，是选招式、
   评估换人是否安全的底层依据。
2. **技能特殊效果（move secondary effects）**——不只是威力数字，还包括异常状态几率、
   能力变化、场地效果（钉子/天气）、反作用力、命中率这些附加信息，直接决定了"这个招式
   除了伤害还带来什么"。
3. **特殊道具（held items）**——比如 Choice 系列（锁招换伤害）、专属变身道具（不可被拍落）、
   Focus Sash（免死一次）这些道具本身自带的规则，会反过来影响"这只精灵该怎么用"。
4. **世代特有机制：太晶化（Terastallize）**——Gen9 独有，一局一次、不可逆的属性切换，
   是这一代最大的博弈点。v1/v2 判断"先不实现"（决策取舍本身值得写进 Introduction/Design，
   呼应"decide what NOT to build"），v3（2026-07-24）已实现范围收紧的 MVP（太晶断死），
   消融验证零回归、机制确认真实生效——这段"先不做→范围收紧后再做"的演变过程本身也是
   "How did your understanding evolve"的好素材，细节见 `Experiment_Log.md` 太晶相关条目。
5. **分级强度体系（Ubers/OU/UU/RU/NU）**——决定了对手队伍的数值强度梯度，是这次任务
   "对手不是单一强度、而是一个五档谱系"这个问题结构的来源。
6. **teampreview 的随机性**——验证过 `poke_env` 的默认实现是随机排轻重出场顺序，
   这条发现直接决定了整个方法论必须是"运行时通用判断"，不能是"记住对手表"。
7. **速度/出手顺序（turn order）**（**2026-07-24 补充**）——谁先手决定招式生效顺序，直接
   影响铺垫招式安不安全、要不要主动撤退这类判断；对手真实数值未知时只能用满配速度上限做
   保守估计。`_effective_speed`/`_we_move_first`/`_max_speed_estimate` 贯穿 `_choose_move`
   决策架构，之前列 factor 清单时漏掉了，翻代码才发现——跟"特性/Ability"不一样（特性只在
   队伍设计阶段被人工考虑过，运行时决策代码里完全没有读 `.ability`，所以不列进这份"运行时
   真正用到的 factor"清单，只适合放进 Reflections 的局限性讨论——2026-07-27 之后局限性讨论
   整个移出 Design，具体归到 Reflections 哪一节还没定，是待办）。

以上几条对应的具体细节分别在 [Game_Mechanics_and_Bots.md](Game_Mechanics_and_Bots.md)
（1、2、3、4、5）和 `bots_strategy.md`（6、7）里已经写清楚了，写 Introduction 时可以直接摘。

---

## Design 部分素材

评分要求：不是"这个方法怎么运作"，而是**为什么这么设计、是否合理、局限性是什么、跟其他方案比怎么样、
理解是怎么演变的**。多个迭代版本可以对比讨论。

**已有素材**：
- `bots_strategy.md` 完整记录了设计推理的演变过程，可以直接当 Design 部分的骨架：
  - 用户最初的直觉方案（Ribombee 开局连招）→ 补充速度优先的验证 → 从 if-elif 优先级链
    演变成"硬短路 + 单回合价值打分"的架构决策，**这个演变过程本身就是报告最想看的东西**
    （"How did it, and your understanding of its design, evolve?"）。
  - 明确记录了"为什么不做"的决策和理由：不做多回合搜索/minimax（对面是非博弈 bot，没必要）、
    第一版不做太晶（变量太多，延后）——这些"为什么不做"跟"为什么做"同样是设计论证的一部分；
    太晶后来在 v3 以范围收紧的 MVP 形式实现（只做"太晶断死"这一种场景），是"先克制延后、
    想清楚范围再做"的一个具体案例。
  - "跨档通用观察"部分（`Opponent_Roster.md` 尾部）解释了为什么规则要写成通用属性判断，
    而不是针对具体精灵打表——可以作为"设计合理性"论证的素材。
- `Game_Mechanics_and_Bots.md` 里"直觉上的难度排序"一节，可以作为"局限性/替代方案"讨论的引子
  （比如为什么没有针对 uber 档单独优化）。

**还缺什么**：
- 目前设计里"权重"（比如铺垫打分的 30 分/级、换人评分的 60 倍率）都是拍脑袋定的初始值，
  报告里如果要体现"quantified metrics"，需要说明这些数字是怎么来的、有没有做过调参/消融实验，
  或者至少诚实说明"这是启发式初始值，第一版没有做系统性调参"（这也是一种合理的局限性讨论）。

### Design 里的"调研 → 发现 → 针对性设计"这条论证链

这是 Design 部分最能体现"为什么这么设计"而不是"这个方法怎么运作"的一段，写作思路：

**第一步，说明调研方法**：针对 Introduction 里提出的"技能特殊效果"这个 factor，
系统性解析了 `bots/teams/` 目录下全部 5 档队伍、30 只精灵的属性/特性/道具/太晶/招式
（对应仓库内 [Opponent_Roster.md](Opponent_Roster.md) 的完整解析工作），而不是等实际对战
遇到了才临时查。

**第二步，报告里具体列出发现的规律**（都已经写在 `Opponent_Roster.md` 的"跨档通用观察"里，
直接摘就行）：
- 拍落（Knock Off）出现频率极高，几乎每档都有 1~2 只会用。
- 撒钉子（隐形岩/撒菱/黏黏网）的对手非常多，覆盖了近一半的对手队伍。
- 电磁波瘫痪在 3 个不同档位重复出现。
- 部分对手带 Choice 系列锁招道具（打出第一招后本回合动作固定）。
- `Zapdos-Galar` 这类带 Defiant 特性的对手，降能力招式对它反而有害。

**第三步，说明这些发现具体怎么反过来影响了设计决策**（这是最关键的一段，体现"针对性想到了
怎么克制"，把发现和设计选择一一对应起来）：
- 因为拍落很常见 → 复查了队伍里哪些精灵的道具是"专属不可拍落"的（Zacian-Crowned 的
  Rusted Sword、Arceus-Ghost/Eternatus 的神石类道具），哪些怕拍落（Kyogre 的 Choice Band、
  Koraidon 的 Loaded Dice），这个认知直接影响了后续"道具被拍掉之后要不要调整打法"这个
  待细化问题的优先级。
- 因为钉子流常见 → 权衡过"专门配一只清钉子/免疫钉子的精灵"和"规则上尽量减少非必要换人"
  两种应对方式，最终选择了后者（详见 `bots_strategy.md`），理由是不想为了应付一个具体机制
  牺牲队伍的输出核心位——**这个取舍本身就是一个"为什么没选替代方案"的现成素材**。
- 因为 Defiant 特性的存在 → 规则里加了"打这类对手时避免用降能力招式"的判断，
  是极少数"针对具体对手的例外规则"，报告里可以拿它跟"通用规则"的设计原则做对比，
  讨论"什么时候值得为一个具体对手写例外、什么时候不值得"这个设计取舍标准。

### Design 里必须坦诚交代的一点：队伍构筑工具的使用

因为团队本身对 Pokémon 对战不算熟悉，为了保证队伍构筑（种族值分配、道具/努力值/性格搭配这些
游戏机制层面的基础）足够扎实、不因为外行判断而拖累后面的评估结果，使用了课程 GitHub 里提供的
[pokeaimmd.com/randomizer](https://www.pokeaimmd.com/randomizer) 辅助搭配出了现在这套阵容，
再在这套阵容基础上设计和实现对战决策逻辑。

这一点建议在 Design 开头（或者单独一个"方法论"小节）明确写出来，是诚实的方法论披露，不是弱点：
**这门课的专家系统考察的是"对战决策逻辑"，不是"团队构筑本身"**，队伍是这次任务的输入数据，
决策逻辑（`_choose_move` 里那套硬短路 + 单回合价值打分的规则系统）才是我们设计和实现的对象。
不交代清楚反而容易让人误以为团队队伍构筑这块也是靠经验直觉，跟实际情况（借助工具 + 针对性调研）
不符。

---

## Evaluation 部分素材

评分要求：**量化指标**，且要证明"系统是按设计运作的"，不能只看"排名好不好"。

**已有素材（`Experiment_Log.md` 三个连续版本，可以直接做成一张对比图/表）**：

| 版本 | 排名 | Mark | 胜场 | 说明 |
|---|---|---|---|---|
| 占位基线（随机+1只精灵） | #16 | 0.0 | 0/15 | 2026-07-21 |
| 满编队伍 + 仍随机选招 | #15 | 1.0 | 1/15 | 隔离"队伍强度"这一个变量 |
| v1 规则系统上线 | — | 9.67（均值） | 14.33/15（均值，3 次重跑） | 硬短路 KO + 单回合打分 + 开局脚本 |

这三个点天然构成一条"控制变量"的实验链——第二步只换队伍、第三步只换决策逻辑，
**分别量化了"队伍强度"和"决策逻辑"各自的贡献**，这正好回应了评分标准里"不能只看最终排名，
要证明方法本身是按设计在起作用"的要求（第二步证明了队伍够强，第三步证明了规则系统确实把
队伍强度兑现成了胜率，两者缺一不可）。

**v1 的消融实验结果**——独立于上面这条主线，专门验证"各个具体机制/权重是不是真的按设计在起作用"，
经过多轮筛选/确认（2026-07-21 单次筛选，随后随着六处 v1 正确性 bug 陆续修复，又各重跑过几轮）
后的最终结论，完整记录见 [Ablation_Study.md](Ablation_Study.md)。**2026-07-24 更新**：修复
`analysis/run_ablation.py` 的进程隔离问题（issue #14）后，全部 9 组配置重新验证，核心结论变成：
**`baseline` 打出满分 15.00/15（stdev=0），跟另外 5 个配置并列**；只有 `no_hard_ko`/
`no_opening_script`（关掉硬短路 KO/开局脚本）才会掉分，且是真实游戏内输赢（对 `simple-uber`），
不是超时；权重类参数（`SETUP_BOOST_WEIGHT`/`SWITCH_DEFENSE_WEIGHT`/`SWITCH_COST`）在测过的范围内
基本不敏感。~~`max_damage-uber` 的输赢是纯粹的 90 秒超时噪声~~这条旧结论是错的——查出来是消融
工具本身的连接残留问题，不是决策逻辑或评测规则的属性，工具修好后这个对手 27 次独立重跑零超时。
这份文档直接回应评分标准里"不能只看整体排名，要证明机制按设计运作"的要求，是 Evaluation 部分
最核心的量化证据来源之一，也是 3.1 节"单次评分 vs. 多次重跑调参可信度"论述的具体实证案例；
"结论后来被推翻"这件事本身是 Reflections 4.5 的核心素材（大纲编号 2026-07-27 因插入新 4.1 顺延）。

**评测方法论本身要在 3.1 讲清楚一个关键区分（2026-07-22 讨论）**：官方判定规则很简单——每个 bot
打 3 局，胜率 >0.5 就算赢，正式评分大概率只跑一次。我们自己做的"多次重跑取均值/标准差"只是**调参
阶段用来判断"这个参数是不是真的更好"的方法**，不会、也不能改变正式评分本身的随机性——正式提交
评的还是单次结果，运气成分始终存在。多次重跑买到的不是"评分那天一定表现好"的保证，而是**"选中的
这套参数平均表现确实更强"的把握，从而提高评分那天抽到好结果的概率**。这个区分本身值得在报告里
讲清楚，也是"减少拖入超时/僵局"（2.1/Design 里的设计目标）这类"直接降低方差"而不只是"绕着方差
调参"的设计动机来源。

**还缺什么（这是目前最大的缺口）**：
- 评分标准明确说"simply placing well... is not enough"——目前的证据基本是"整体胜率变化"，
  还缺**机制级别的验证**，比如：
  - 硬短路 KO 规则实际触发了几次？触发时判断对了吗（有没有"以为能斩杀结果没斩杀"的误判）？
  - 换人评分选的对象，事后看是不是真的是更优解（比如复盘 `simple-ou`/`simple-uu` 这两场输局，
    看是换人评分算错了，还是铺垫时机判断错了，还是纯粹打不过）？
  - 这需要看 `replays/wche652/` 里的具体对局记录，是 `bots_strategy.md` 待办清单里第一项，
    也是接下来最优先要补的 Evaluation 素材。
- 目前只测过一次每个版本（`n_challenges=3`），样本量对报告里的"量化"说服力偏薄，
  可以考虑对最终版本多跑几轮取更稳定的胜率区间。

**追踪基础设施（2026-07-21 已实现）**：为了拿到上面这些机制级证据，需要能回看
"每回合双方到底做了什么、我们当时为什么这么选"。关键发现：`poke_env` 的 `Battle.observations`
已经按回合把双方发生的所有事件（招式/伤害/异常状态变化）记好了，战斗结束后还能从
`player.battles`（`{battle_tag: Battle}`）整场取出来，**这部分不需要我们自己实现**。
真正需要自己加的，只有"我们自己的打分内部逻辑"这一层（poke_env 不知道我们的评分细节）：

- `wche652.py` 里给 `CustomAgent` 加一个 `self.decision_log`（纯内存 list，不涉及磁盘写入，
  不会触发 `file_write_detection.py`），每回合记一条：回合数、双方精灵/血量/状态、走了哪个分支
  （开局脚本/硬短路 KO/常规打分）、候选动作和各自分数、最终选择。
- 另外写一个**不在 `players/` 目录里**的分析脚本（建议放仓库根目录 `analysis/`，不属于提交范围，
  不受写盘限制），复用 `expert_main.py` 的 `gather_bots`/`evaluate_player_vs_bot`，跑完后把
  `player.decision_log` 和 `battle.observations` 按 `battle_tag`/回合号拼起来落盘成 CSV，
  用于复盘和消融实验（权重扫描：铺垫打分权重、换人评分权重、`SWITCH_COST`、安全阈值等）。

`decision_log` 每回合一条，建议字段（聊天记录里定的版本，写在这里避免以后要回翻对话）：

| 字段 | 内容 |
|---|---|
| `battle_tag` | 对战唯一 ID，用于跟 `battle.observations`/最终胜负结果关联 |
| `turn` | 回合数 |
| `my_pokemon` / `my_hp_fraction` / `my_status` | 我方当前出战精灵、血量、异常状态 |
| `opp_pokemon` / `opp_hp_fraction` / `opp_status` | 对面当前出战精灵、血量、异常状态 |
| `branch` | 走了哪条代码路径：`ribombee_opening` / `hard_ko` / `general_scoring` / `forced_switch` |
| `candidates` | `general_scoring` 分支里比较过的所有选项，每个是 `(动作类型, 名字, 分数)` |
| `chosen_action` | 最终选择的动作 |
| `chosen_score` | 最终选择的分数（硬短路分支为 `None`） |
| `we_move_first` | 这回合速度判断结果，专门用来复盘铺垫时机判断得对不对 |

消融实验维度（对应代码里现有的几个硬编码常量）：硬短路 KO 规则开关、Ribombee 固定开局脚本开关、
铺垫打分权重（现在每级 30 分）、换人评分的防御风险权重（现在 ×60）、`SWITCH_COST`（现在 25）、
铺垫安全阈值（先手 0.5 / 后手 0.35）——每组配置跑一次 15-bot 梯队，记录 mark/排名/分档胜率对比。

实现落地在：`wche652.py` 顶部一批模块级常量（`ENABLE_HARD_KO`/`ENABLE_RIBOMBEE_OPENING`/
`SETUP_BOOST_WEIGHT`/`SWITCH_DEFENSE_WEIGHT`/`SWITCH_COST`/`FASTER_SURVIVAL_THRESHOLD`/
`SLOWER_SURVIVAL_THRESHOLD` 等）+ `self.decision_log`；`analysis/run_ablation.py` 负责热加载
模块、按配置覆写常量、跑梯队、把 `decision_log` 和 `battle.observations` 落盘到
`analysis/results/<config>/`。冒烟测试（跑 `baseline` 一组）已经验证整条链路可用。

---

## Reflections 部分素材

评分要求：预期 vs 实际的对比、什么起作用了什么没有、为什么、专家系统本身的局限性体会。

**已有素材**：几乎没有——目前 `Experiment_Log.md` 的"观察/结论"部分偏 Evaluation（解释这轮结果说明了
什么），还没有专门反思"这跟我们最初的预期比怎么样"这类内容。

**建议从现在开始补的角度**（可以直接作为以后写 Experiment_Log.md 条目时多问自己的问题）：
- 队伍强度实验那轮已经隐含了一个很好的反思素材：**"纯规则、非学习的方法，遇到会博弈的 `simple`
  档明显比不会博弈的 `random`/`max_damage` 吃力"**——这其实正好呼应"专家系统的局限性"这个课程主题
  （手写规则很难覆盖所有对手行为模式，尤其是对手本身有一定适应性的时候），值得在 Reflections 里
  展开讨论，对应"你对专家智能体本质有什么新理解"这个问题。
- 后续排查 `simple-ou`/`simple-uu` 失败原因时，如果发现是"设计时想当然、实测才发现不成立"的情况
  （比如打分权重不合理、速度估算不准），这类"预期落空"的具体案例是 Reflections 最好的素材，
  发生的时候记得当场记下来，不要事后凭印象回忆。

**已经抓到一个真实案例（2026-07-21，冒烟测试第一场战斗就发现了）**：`_effective_speed` 对我方用
真实计算过的速度（含 EV/性格），对对手因为不知道真实努力值分配，退化成只用种族值估算。
实测中 vs `simple-uber-1`（对面开局 Zacian-Crowned），`decision_log` 显示第 2 回合我们判断
`we_move_first = True` 选择 U-turn 撤退，但 Ribombee 当场阵亡（U-turn 根本没打出去）——
说明实际是对面先手。原因是这套 bot 队伍给 Zacian-Crowned 拉了 252 速度努力值+慢劲性格，
真实速度（约 414）远高于种族值（148），而我们的估算方式只用了种族值，系统性低估了
"认真堆速度"的对手，导致"以为自己先手"这个判断在这类对手身上出错，且这次出错的代价是
丢了开局节奏（Ribombee 提前阵亡）。这是一个现成的、有决策日志证据支撑的"设计假设 vs
实测结果不符"案例，可以直接写进 Reflections，讨论"用有限信息（种族值）近似未知量（对手真实
努力值）"这个专家系统里常见的权衡——完全准确需要更多假设/计算成本，退而求其次的近似在
"认真配置"的对手身上会系统性出错。

---

## 报告章节 → 仓库文件对照表

| 报告部分 | 主要来源文件 |
|---|---|
| Introduction | 本文档"Introduction 部分素材"一节；背景细节见 `Game_Mechanics_and_Bots.md` |
| Design | `bots_strategy.md`（主）、`Opponent_Roster.md` / `Game_Mechanics_and_Bots.md`（背景依据）、
本文档"队伍构筑工具的使用"一节（方法论披露） |
| Evaluation | `Experiment_Log.md`（主，含量化数据表）、[Ablation_Study.md](Ablation_Study.md)
（单次筛选轮 9 组 + 多次重跑确认轮 7 组，均已跑完）、`replays/wche652/`、`analysis/results/`
（原始 CSV 数据） |
| Reflections | 目前无专门文件，边做边记，之后可能需要单独开一个 `Reflections.md` 或并入 `Experiment_Log.md` |

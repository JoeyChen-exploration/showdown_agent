# 消融实验记录

> 用 `analysis/run_ablation.py` 对 `players/wche652.py` 的几个关键常量/开关做的消融实验。
> 每组配置跑一次完整 15-bot 梯队（`n_challenges=3`），改一个参数、其余保持默认。原始数据在
> `analysis/results/<config>/`（`per_bot.csv`/`decisions.csv`/`events.csv`），这份文档是对结果的
> 解读和记录。**2026-07-23 文件改名**：原名 `Ablation_Study_v1.md`，内容按时间顺序从 v1 一路记到
> v2，改回不带版本号的名字更准确（issue #1）；旧文件名的历史链接如果还有地方引用，指向的是同一份
> 内容，只是路径变了。

> 换人打分公式做过一次修正（详见 `Experiment_Log.md` 2026-07-22 条目），下面"2026-07-22 多次重跑
> 确认结果（修正前...）"一节的数据已经过时，仅作历史对照；当前有效数据在文档末尾
> "2026-07-22 换人打分修正后，重新确认结果"一节。

## 配置列表

| 配置名 | 改动 |
|---|---|
| `baseline` | 不改，v1 默认值 |
| `no_hard_ko` | 关闭硬短路 KO 规则（`ENABLE_HARD_KO=False`） |
| `no_opening_script` | 关闭 Ribombee 固定开局脚本（`ENABLE_RIBOMBEE_OPENING=False`） |
| `setup_weight_15` / `setup_weight_45` | 铺垫打分权重从默认 30 改成 15 / 45 |
| `switch_defense_weight_30` / `switch_defense_weight_90` | 换人评分的防御风险权重从默认 60 改成 30 / 90 |
| `switch_cost_0` / `switch_cost_50` | 换人成本从默认 25 改成 0 / 50 |

## 结果总表

| 配置 | 排名 | Mark | 胜场 | 输给了谁 |
|---|---|---|---|---|
| **baseline** | #2 | 9.5 | 14/15 | `simple-uu` |
| no_hard_ko | #3 | 9.0 | 13/15 | `simple-uu`、`max_damage-uber` |
| no_opening_script | #2 | 9.5 | 14/15 | `max_damage-uber` |
| setup_weight_15 | #3 | 9.0 | 13/15 | `simple-uu`、`max_damage-uber` |
| **setup_weight_45** | #4 | 8.5 | 12/15 | `simple-uu`、`simple-nu`、`max_damage-uber` |
| switch_defense_weight_30 | #2 | 9.5 | 14/15 | `max_damage-uber` |
| switch_defense_weight_90 | #2 | 9.5 | 14/15 | `max_damage-uber` |
| switch_cost_0 | #2 | 9.5 | 14/15 | `max_damage-uber` |
| switch_cost_50 | #3 | 9.0 | 13/15 | `simple-uu`、`max_damage-uber` |

## 读出来的信号

1. **硬短路 KO 规则有正贡献**：`no_hard_ko` 比 `baseline` 少赢一场（13/15 vs 14/15）。即使这条规则
   本身有已知瑕疵（不看招式威力，见 `1st_strategy.md` 待细化清单），关掉它整体表现仍然更差，
   说明"能斩杀就直接斩杀"这个短路判断本身方向是对的，瑕疵不影响它净贡献为正。
2. **铺垫打分权重像是有个"甜区"**：默认值 30 两侧（15 和 45）表现都更差，其中调高到 45 是全场
   最差的一组（12/15，多输了 `simple-nu`）。说明默认值不是拍脑袋碰巧对的，而是调得太激进
   （更频繁地铺垫）反而会在不该铺垫的时候浪费回合，这是个值得写进报告 Evaluation/Design 部分的
   具体量化证据。
3. **换人相关的两个参数在测试范围内不敏感**：`SWITCH_DEFENSE_WEIGHT`（30/60/90）和
   `SWITCH_COST=0`（vs 默认 25）跟 baseline 结果完全一样（都是 14/15，输给 `max_damage-uber`）。
   `SWITCH_COST=50` 才开始表现变差。说明当前胜率的瓶颈可能不在换人逻辑这个方向，继续往这个方向
   调权重边际收益可能有限。
4. **`simple-uu` 和 `max_damage-uber` 是两个"摇摆"对手**：9 组配置里反复在赢/输之间切换，
   是目前最不稳定的两个对手，值得优先复盘 `decision_log` 看具体输在哪个环节。
5. **`no_opening_script` 跟 `baseline` 战绩打平（都是 14/15），但输的对手不一样**（`baseline` 输
   `simple-uu`，`no_opening_script` 输 `max_damage-uber`）——这不能直接解读成"开局脚本没用"，
   更可能是**样本量太小**（每组配置只跑了一次 15-bot 梯队，每场只有 3 局）导致的噪声，
   跟 `Report_Outline.md` 里已经记过的"样本量偏薄"缺口是同一个问题。要真正判断开局脚本的贡献，
   需要同一配置多跑几轮取胜率区间，而不是单次结果直接比较。

## `max_damage-uber` 超时问题（规则内的真实约束，不是要绕开的东西）

> **⚠️ 2026-07-23 更新**：下面"对面带 Arceus-Fairy 冥想+回血难磨穿"这条根因判断**已经被推翻**。
> 后续验证发现（1）`max_damage-uber` 这个 AI 风格源码只会"选威力最高的招式"，冥想/回血威力都是
> 0，实际从来不会被这个 bot 用到；（2）我方对 Arceus-Fairy 至少有两条独立的超克路线（Eternatus
> 毒系、Zacian-Crowned/Koraidon 钢系都是 2 倍），实战里经常两三回合就能解决它。围绕"我方打不动/
> 资源没留对"这个假设做的两版 v2 功能（Taunt 反叠盾、克制对照表）都没能改变这场的胜负数字一分——
> 详见 `Experiment_Log.md` 2026-07-23 对应条目。当前判断是：这场超时更可能是 Bo3 三局总时长
> 单纯偶尔超支的时长/延迟问题，跟"这场具体打不打得动"关系不大。下面的历史分析保留作记录，
> 但"面对高耐久回血流对手时的专门判断"这个方向已经验证过效果不明显，不建议继续投入。

**现象**：9 组配置里，8 组在 `max_damage-uber` 这场超时判负（`MATCH_TIMEOUT_SECONDS=90` 秒内 3 局
打不完），只有 `baseline` 这一组赢了。查 `baseline` 自己打这场的 `decision_log`，回合数跑到
39-42 回合——远超一般对局的 15-30 回合，说明这个对局（对面带 `Arceus-Fairy`，冥想+生命种子，
很难磨穿）本身容易演变成拖很久的拉锯战，天然贴近 90 秒墙钟的边缘，配置之间的微小差异就可能
决定是否卡进超时。

**处理方式（已确认）**：`MATCH_TIMEOUT_SECONDS` 是 `expert_main.py` 里的评测规则常量，
正式评分用的就是这个 90 秒——**不在我们自己的分析脚本里放宽它**，即使只是为了让本地消融数据
更"干净"也不改，因为那样测出来的表现不代表真实评测环境下会发生什么。90 秒当成硬约束、
当成需要在策略层面解决的问题：**如果 v2 迭代要处理这个问题，方向应该是"让打法更果断、
避免陷入拉锯战"**（比如面对高耐久回血流对手时的专门判断），而不是想办法拖长时间限制。

## 这一轮的定位：筛选实验，不是最终调参结论

这 9 组配置每组只跑了一次，**目的是低成本筛出哪些变量真的敏感、值得投入更贵的多次重跑去精调**，
不是直接给出"最优参数值"。按这个目的分类：

- **敏感，值得优先精调**（对应 GitHub issue #8）：
  - `ENABLE_HARD_KO`——关掉少赢一场，有清楚信号。
  - `SETUP_BOOST_WEIGHT`——默认值 30 两侧（15、45）都更差，明显的"甜区"形状。
  - `SWITCH_COST`——50 掉一场，0 没变化，方向上有信号。
- **看起来不敏感，先不投入更多精调预算**：
  - `SWITCH_DEFENSE_WEIGHT`——30/60/90 结果完全一样。
- **信号被单次结果的噪声盖住，需要多次重跑才能判断，不能现在下结论**：
  - `ENABLE_RIBOMBEE_OPENING`——`no_opening_script` 跟 `baseline` 战绩打平（都 14/15），但输的
    对手不一样，说不清是真的没用还是运气。

## 下一步（筛选轮，历史记录，已被下面的多次重跑轮部分取代）

1. 复盘 `simple-uu`（贯穿多组配置的摇摆对手）和 `max_damage-uber`（超时问题）这两场的
   `decision_log`，具体定位输/超时的回合和原因。（GitHub issue #6）
2. 消融实验补充多次重跑机制（GitHub issue #2），优先用在上面筛出来的敏感变量上
   （`ENABLE_HARD_KO`/`SETUP_BOOST_WEIGHT`/`SWITCH_COST`），以及信号不明确的 `ENABLE_RIBOMBEE_OPENING`。
3. v2 迭代时把"如何更快结束拉锯战"作为一个专门的设计目标，直接回应这次发现的超时问题。
   （GitHub issue #4）

---

## 2026-07-22 多次重跑确认结果（修正前，已被下方新一轮取代，保留作历史对照）

对筛选轮标记为"敏感"或"信号不明确"的 7 组配置（`baseline`/`no_hard_ko`/`no_opening_script`/
`setup_weight_15`/`setup_weight_45`/`switch_cost_0`/`switch_cost_50`）各重跑 3 次完整
15-bot 梯队，取均值/标准差。`switch_defense_weight_30/90`（筛选轮判定不敏感）没有重跑，
沿用筛选轮的单次数据。原始数据：`analysis/results/<config>/per_bot_summary.csv`（每个对手
的均值/标准差胜率）、`analysis/results/summary.csv`（每个配置的聚合结果）。

### 结果总表

| 配置 | mean_mark | stdev_mark | mean_beaten/15 | 胜场区间 |
|---|---|---|---|---|
| **baseline** | 9.17 | 0.29 | 13.33 | 13–14 |
| no_hard_ko | 9.00 | 0.00 | 13.00 | 13–13 |
| **no_opening_script** | **9.50** | **0.00** | **14.00** | 14–14 |
| **setup_weight_15** | **9.50** | **0.00** | **14.00** | 14–14 |
| setup_weight_45 | 8.83 | 0.29 | 12.67 | 12–13 |
| switch_cost_0 | 9.00 | 0.00 | 13.00 | 13–13 |
| switch_cost_50 | 9.33 | 0.29 | 13.67 | 13–14 |

### 读出来的信号（更新/取代筛选轮的对应结论）

1. **`max_damage-uber` 的输赢在这些参数上完全不敏感，筛选轮 baseline 那次"赢"是噪声**：
   `baseline` 单次跑出的 1/3 胜场（mean_winrate=0.33, **stdev=0.58**，3 次重跑里有明显分歧）是
   目前全表标准差最大的一格；而其余全部 6 组配置都是稳定的 **0/3**（stdev=0）。也就是说，筛选轮
   报告里"9 组配置 8 组输给 `max_damage-uber`、只有 baseline 赢"这个对比，看起来像 baseline
   有什么特殊优势，实际上更可能是 baseline 那一次单次跑（那时候还没有多次重跑机制）刚好卡在
   90 秒超时墙的有利一侧，纯属运气。**这进一步确认了本文档前面"超时问题"一节
   的判断**：这场对局的胜负目前主要由是否卡进 90 秒超时决定，不由这 7 个参数决定，需要 issue #4
   那种"让打法更果断"的结构性改动才能真正改善，继续在这几个权重上找也没用。
2. **`ENABLE_RIBOMBEE_OPENING` 的信号被筛选轮的噪声掩盖了，多次重跑后是清楚的正向信号**：
   筛选轮判定"打平、说不清"，但重跑 3 次后 `no_opening_script` 稳定在 **14/15（stdev=0）**，
   比 `baseline` 的 13.33/15（stdev=0.29）高了一整场，而且区间不重叠（14–14 vs 13–14 有一点点
   重叠但均值方向一致、标准差趋近于 0）。机制上看是一次**换手交易**：关掉开局脚本后 `simple-uu`
   从稳定输（baseline: mean_winrate=0.33, 0/3）变成稳定赢（mean_winrate=1.00, 3/3），代价是
   `max_damage-uber` 从偶尔能赢（baseline 1/3）变成稳定输（0/3）——但上一条已经确认这场本来就
   是超时噪声决定的，不是真的因为开局脚本，所以这笔交易接近纯赚：多赢一场稳定的 `simple-uu`，
   没有真正牺牲什么。
3. **`SETUP_BOOST_WEIGHT` 的"甜区"形状被重跑确认了，但下边界比默认值 30 更好**：
   `setup_weight_15` 同样稳定在 14/15（stdev=0），机制跟 `no_opening_script`几乎一样——也是
   `simple-uu` 从稳定输变成稳定赢（mean_winrate=0.89, 3/3）。`setup_weight_45` 则被重跑坐实是
   全场最差（12.67/15），而且不只是重复筛选轮的"多输 `simple-nu`"，重跑后看到的是 `simple-uu`
   稳定输（跟 baseline 一样 mean_winrate=0.33）**外加** `max_damage-uu` 开始不稳定
   （mean_winrate=0.67, stdev=0.33, 2/3）——铺垫权重调太高不仅没有在原本就输的对手上翻盘，
   反而在原本稳赢的对手上引入了新的不确定性。
4. **`SWITCH_COST` 方向不再像筛选轮那样清楚，重跑后接近中性偏正**：`switch_cost_50` 均值
   13.67/15，比 baseline 的 13.33 略高，但标准差没降到 0（区间还是 13–14）——细看是因为
   `simple-uu` 变成稳定赢（跟前两条同一个模式，mean_winrate=0.67, 3/3）的同时，新引入了
   `simple-ou` 的不稳定（mean_winrate=0.78, **stdev=0.38，全表第二大**，2/3）。`switch_cost_0`
   跟 baseline 比是稳定变差（13/15, stdev=0，`simple-uu` 稳定输且 winrate 比 baseline 更低）。
   跟筛选轮"50 掉一场、0 没变化"的结论方向一致，但现在看两个方向都不是免费的：调高换人成本用
   `simple-ou` 的稳定性换 `simple-uu` 的稳定性，不是单纯的正向改动。
5. **一个值得跟进的观察：三种不同机制的改动（关闭开局脚本 / 调低铺垫权重 / 调高换人成本）都独立地
   把 `simple-uu` 从稳定输翻成稳定赢**，说明 baseline 输给 `simple-uu` 可能不是某一个单一原因，
   而是这几个参数在当前默认值组合下共同把某类招式选择推向了错误方向。三条路径改的是完全不同的
   决策环节（开局脚本走的是固定脚本分支，铺垫权重和换人成本都在 `_best_scored_action` 的打分
   公式里，但影响的是不同候选动作类别），却指向同一个对手结果，值得复盘 `simple-uu` 的
   `decision_log`（issue #6）具体看这几组配置在同一回合选了什么不同的动作，而不是只看聚合胜负。

### 更新后的分类（喂给 issue #8：打分公式权重的系统性校准）

- **确认敏感、且方向明确，值得当作 v2 默认值候选**：
  - `ENABLE_RIBOMBEE_OPENING=False`（关闭开局脚本）—— 14/15, stdev 0，目前样本里最干净的正向信号。
  - `SETUP_BOOST_WEIGHT=15`（下调，而不是维持默认 30）—— 同样 14/15, stdev 0。
  - 两者效果高度相似（同样的机制：`simple-uu` 翻盘），下一轮值得测试**两者叠加**是否有更大收益，
    还是彼此重叠（issue #8 的具体任务之一）。
- **确认敏感、方向明确是负向**：`SETUP_BOOST_WEIGHT=45`（上调）—— 12.67/15，全场最差，不用再测。
- **确认对这 7 个参数不敏感，需要结构性方案**：`max_damage-uber` 的超时输赢——不再往这几个权重
  上投入精力，转给 issue #4（更快结束拉锯战）。
- **方向不再清楚，需要更细的按对手拆分数据才能判断是否值得改**：`SWITCH_COST`——0 和 50 都各自
  在不同对手上有得有失，不是单纯"越大越好/越小越好"。

---

## 2026-07-22 换人打分修正后，重新确认结果（已被下方新一轮取代，保留作历史对照）

换人评分公式里一处方向性问题得到修正后，重新对同样 7 组配置各跑 3 次完整 15-bot 梯队。这是当前
有效的消融数据，上面"修正前"那一节仅作历史对照，不代表当前系统的真实行为。

### 结果总表（括号内为修正前的对比值）

| 配置 | mean_beaten/15 | stdev | 胜场区间 | 修正前 mean_beaten | 修正前 stdev |
|---|---|---|---|---|---|
| baseline | 13.67 | 0.76 | 12–15 | 13.33 | 0.29 |
| no_hard_ko | 13.67 | 0.29 | 13–14 | 13.00 | 0.00 |
| no_opening_script | 13.67 | 0.29 | 13–14 | 14.00 | 0.00 |
| **setup_weight_15** | **14.00** | **0.00** | 14–14 | 14.00 | 0.00 |
| **setup_weight_45** | **14.00** | **0.00** | 14–14 | 12.67 | 0.29 |
| switch_cost_0 | 13.67 | 0.29 | 13–14 | 13.00 | 0.00 |
| switch_cost_50 | 13.67 | 0.29 | 13–14 | 13.67 | 0.29 |

### 读出来的信号

1. **整体胜场普遍上移**：7 组里 6 组 mean_beaten 比修正前涨了，两组（`setup_weight_15`/
   `setup_weight_45`）涨到全场并列最高的 14.00（stdev 0）。
2. **`simple-uu` 不再是"摇摆对手"**：修正前 7 组配置里明显分裂成"稳定输"和"稳定赢"两派；
   修正后 6/7 组都能稳定赢下这场（beaten_count 2-3/3），不再是配置之间的分歧点。
3. **`SETUP_BOOST_WEIGHT` 的"甜区"结论不成立了**：修正前数据显示默认值 30 两侧（15/45）都更差、
   45 是全场最差；修正后 **15 和 45 并列最好**，完全不是单侧甜区的形状，之前那个结论作废。
4. **`no_opening_script` 不再是一个能单独拿出来讲的正向发现**：修正前它跟 `setup_weight_15`
   并列最优；修正后回落到跟 `baseline` 同一档（13.67），没有比不改动更好。
5. **`max_damage-uber` 这场的模式完全没变**：只有 `baseline` 偶尔能赢（winrate 均值 0.33，
   stdev 0.58），其余 6 组稳定输（0/3），数字几乎跟修正前一样——再次确认这场的胜负由 90 秒超时
   决定，不受这 7 个参数影响，继续在这几个权重上调没有意义。
6. **`baseline` 自己的标准差变大了**（0.29 → 0.76，胜场区间从 13–14 变宽到 12–15），暂时不确定
   是真实效应还是 3 次重跑样本太小的噪声，需要更多重跑才能判断。

### 结论（喂给 issue #8：打分公式权重的系统性校准）

- `setup_weight_15`/`setup_weight_45` 目前并列最优，样本还不够分出哪个更稳，值得下一轮重点跑
  更多次重跑区分。
- `no_hard_ko`/`no_opening_script`/`switch_cost_0`/`switch_cost_50` 现在跟 `baseline` 处在
  同一档，没有哪个能单独当作"确认更好"的改动。
- `SWITCH_DEFENSE_WEIGHT`（30/60/90，之前判定"不敏感"）当时也是在同一套换人打分逻辑下测的，
  这个"不敏感"结论同样不可信，需要找机会重新测。
- `max_damage-uber` 的超时问题维持不变的结论：不指望调权重解决，转 issue #4 的结构性方案。

## 下一步（上一轮，已被下方取代）

1. 对 `setup_weight_15`/`setup_weight_45` 做更多次重跑，确认哪个更稳（issue #8）。
2. 重新测 `SWITCH_DEFENSE_WEIGHT`（30/60/90），之前的"不敏感"结论建立在旧的换人打分逻辑上，
   不能直接沿用（issue #8）。
3. `max_damage-uber` 的超时问题转到 v2"更快结束拉锯战"的专门设计，不再指望调权重解决（issue #4）。

---

## 2026-07-23 三处 v1 正确性修复（换人打分 + 对手速度估算 + KO 威力盲区）后，重新确认结果（已被下方新一轮取代，保留作历史对照）

在上一轮换人打分修正的基础上，又修了两处已知 bug：对手速度估算（之前用种族值原始数字，系统性
偏低）、硬短路 KO 判断（之前没看招式威力）。三处修复一起生效后，对同样 7 组配置重新各跑 3 次
完整 15-bot 梯队。这是当前有效的消融数据，上面两轮"修正前/修正后"仅作历史对照。

### 结果总表（括号内为上一轮——只修了换人打分——的对比值）

| 配置 | mean_beaten/15 | stdev | 上一轮 mean_beaten |
|---|---|---|---|
| **baseline** | **14.00** | 0.50 | 13.67 |
| **no_hard_ko** | **14.00** | 0.00 | 13.67 |
| no_opening_script | 13.00 | 0.00 | 13.67 |
| **setup_weight_15** | **14.00** | 0.00 | 14.00 |
| setup_weight_45 | 13.67 | 0.29 | 14.00 |
| switch_cost_0 | 13.67 | 0.29 | 13.67 |
| **switch_cost_50** | **14.00** | 0.00 | 13.67 |

### 读出来的信号

1. **整体胜场进一步上移**：4/7 组配置（`baseline`/`no_hard_ko`/`setup_weight_15`/`switch_cost_50`）
   并列最高 14/15，比上一轮（只有 2 组并列最高）更多配置达到这个水平。
2. **`SETUP_BOOST_WEIGHT` 只剩 `15` 这个方向站得住**：`setup_weight_15` 连续两轮独立重跑都拿到
   14/15，是目前唯一一个跨轮次都稳定更好的具体改动；`setup_weight_45` 从上一轮的并列最优跌回中间档
   （13.67），不再有正面证据。
3. **`no_opening_script` 变成全场最差**（13/15）：细看是 `simple-uber` 这个对手上出问题
   （胜率均值 0.11，3 次重跑 0/3）。这个配置三轮消融里分别是"打平→最优→最差"，是波动最大的一个，
   不建议当作确认过的改进方向。
4. **`simple-uu` 不再是任何配置的弱点**：7 组配置这次全部是多数胜（≥2/3）。`simple-uber` 变成
   新的、跨配置波动最大的对手（从 0/3 到 3/3 都有），是下一轮值得复盘的对象。
5. **`max_damage-uber` 的模式三轮消融里完全没变过**：只有 `baseline` 偶尔赢（这次 1/3，
   stdev 0.58），其余配置稳定输（0/3）——连续三轮、跨越三处代码修复都没变化，是目前证据最充分的
   结论：这场的胜负纯粹由 90 秒超时决定，不受这些参数影响。

### 结论（当前有效数据，喂给 issue #8）

- **`SETUP_BOOST_WEIGHT=15`** 是目前唯一一个在两轮独立重跑里都确认更好的改动，值得当作下一版
  默认值候选。
- `ENABLE_HARD_KO`（默认开启）这轮里也进了最高档，没有新证据支持关掉它。
- `no_opening_script`/`switch_cost_0`/`setup_weight_45` 方向都不稳定（跨轮次结论会变），不建议
  现在下结论——这几个参数的"最优值"看起来对系统其它部分的改动很敏感，需要固定住其它变量后再单独测。
- `max_damage-uber` 的超时问题维持不变的结论：不指望调权重解决，转 issue #4 的结构性方案。

## 2026-07-23 `SWITCH_DEFENSE_WEIGHT` 重测（三处修复后，issue #8）

旧的"30/60/90 结果完全一样、不敏感"结论是在 `_switch_score` 还有方向反了的 bug 时测的，不可信。
用当前修复后的代码重新测 30 和 90（默认值 60 = `baseline`，已经有数据，不用重跑），各 3 次重跑：

| 配置 | mean_beaten/15 | stdev |
|---|---|---|
| `switch_defense_weight_30` | 14.00 | 0.50 |
| `baseline`（即 60） | 14.00 | 0.50 |
| `switch_defense_weight_90` | **14.00** | **0.00** |

**结论**：30/60/90 三个值现在**全部并列拿到 14/15**，`switch_defense_weight_90` 的标准差甚至是
0（比 baseline 自己还稳）。旧的"不敏感"结论修复后重新验证依然成立——只是原因不一样：不是因为
公式本身没有区分度（旧数据是这样），而是**换人打分公式修正之后，这个权重在 30-90 这个范围内确实
不太影响最终胜场**。不建议在这个参数上继续投入精调预算，跟 issue #8 之前对这个参数的判断一致，
现在有可信的数据支撑了。

## 下一步（上一轮，已被下方取代）

1. 复盘 `simple-uber` 的 `decision_log`（尤其是 `no_opening_script` 那组为什么 0/3），这是当前
   最大的未解释信号。
2. `max_damage-uber` 的超时问题转到 v2"更快结束拉锯战"的专门设计，不再指望调权重解决（issue #4）。
3. `SETUP_BOOST_WEIGHT=15`、`ENABLE_HARD_KO=True`（默认）作为下一版默认值候选，`SWITCH_DEFENSE_WEIGHT`
   维持默认值 60（三个测试值里无差异，没必要改）。

---

## 2026-07-23 六处 v1 正确性修复后，最终结果（v1 阶段性收尾，已被下方 v2 一轮取代，保留作历史对照）

在上一轮基础上又修了三处：Judgment（Arceus-Ghost 招牌招式）类型没跟着阵型动态解析、
Scale Shot（Koraidon 核心招式）多段攻击威力低估约 3 倍、钉子招式目标场地判断有隐患。
六处修复一起生效后重新跑完 7 组配置 × 3 次重跑，是目前 v1 最终、最可信的一轮数据。

### 结果总表（括号内为上一轮——三处修复——的对比值）

| 配置 | mean_beaten/15 | stdev | 上一轮 mean_beaten |
|---|---|---|---|
| **baseline** | **14.33** | 0.29 | 14.00 |
| no_hard_ko | 14.00 | 0.00 | 14.00 |
| no_opening_script | 13.00 | 0.00 | 13.00 |
| setup_weight_15 | 13.67 | 0.29 | 14.00 |
| setup_weight_45 | 14.00 | 0.00 | 13.67 |
| switch_cost_0 | 14.00 | 0.00 | 13.67 |
| switch_cost_50 | 14.00 | 0.00 | 14.00 |

### 读出来的信号

1. **`baseline`（全部参数用默认值）首次成为全场单独最优**（14.33/15，3 次重跑里有一次打出
   15/15 满分）——四轮消融里第一次出现这个情况。Judgment/Scale Shot 修复直接让 Koraidon/
   Arceus-Ghost 这两只精灵的核心输出手段被正确估算，核心系统本身变强了，不再需要靠调整这几个
   权重去弥补。`simple-uber` 这场也从之前几轮的 2-3/3 摇摆变成 `baseline` 稳定 3/3——正是
   这两只精灵在这局的主力输出。
2. **`SETUP_BOOST_WEIGHT` 的"最优值"第三次反转**（15→45→15→45，没有任何一个非默认值连续
   两轮站得住），而默认值 30（即 `baseline`）现在是单独最高分——**不再建议改这个参数**，
   之前"15 更优"的结论作废。
3. **`no_opening_script` 连续两轮稳定卡在 13/15**（stdev 都是 0）——这六处修复都没碰 Ribombee
   相关逻辑，跟之前复盘的结论一致：Focus Sash 缓冲 + `DEFAULT_ATTACK_ASSUMPTION_BP` 偏低共同
   导致，不是这几个已修的 bug 造成的，需要另外处理（见下）。
4. **`max_damage-uber` 的模式连续四轮、跨六处代码修复完全没变过**：只有 `baseline` 偶尔赢
   （1/3，stdev 0.58，数字几乎和第一轮一模一样），其余配置稳定输——是整个研究里证据最扎实的
   结论，纯粹是 90 秒超时问题，不受任何已测参数影响。

### 结论（v1 阶段性收尾）

- **v1 默认配置目前是综合表现最好的版本**：`ENABLE_HARD_KO=True`、`ENABLE_RIBOMBEE_OPENING=True`、
  `SETUP_BOOST_WEIGHT=30`、`SWITCH_DEFENSE_WEIGHT=60`、`SWITCH_COST=25` 全部维持默认值，
  不再基于这几轮的消融结果改动——六处 bug 修复本身就是目前最大的提升来源，参数微调的边际收益
  比预期小。
- 仅剩两个已知、已解释、暂不通过调参解决的缺口：`no_opening_script` 的 `simple-uber` 回归
  （issue #7/#8）、`max_damage-uber` 的超时问题（issue #4）。

## 下一步（上一轮，已被下方取代）

1. v1 阶段性告一段落，转向 issue #9/#11（Report Design/Reflections）或 issue #7/#4/#5 这些
   更大的方向。
2. `DEFAULT_ATTACK_ASSUMPTION_BP=90` 值得重新校准（issue #8），是 `no_opening_script`
   回归背后的一个具体因子。
3. `max_damage-uber` 的超时问题转到 v2"更快结束拉锯战"的专门设计（issue #4）。

---

## 2026-07-23 v2（Taunt 反叠盾 + 克制对照表 + 延迟收益打分）后，最终结果（当前有效数据）

v1 阶段性收尾之后正式进入 v2：加了 Taunt 反叠盾评分、团队预览克制对照表、钉子/减益壁类招式的
"延迟收益"打分（用"对面/己方还剩几只精灵可能换上来"取代原来固定 35 分）。三个都是通用机制，
不是针对某个具体 bot 硬编码。详细设计和验证过程见 `Experiment_Log.md` 对应条目。

### 结果总表（括号内为六处修复轮的对比值）

| 配置 | mean_beaten/15 | stdev | 六处修复轮 mean_beaten |
|---|---|---|---|
| **baseline** | **14.33** | 0.29 | 14.33 |
| no_hard_ko | 14.00 | 0.00 | 14.00 |
| no_opening_script | 13.00 | 0.00 | 13.00 |
| setup_weight_15 | 14.00 | 0.00 | 13.67 |
| setup_weight_45 | 14.00 | 0.00 | 14.00 |
| switch_cost_0 | 13.67 | 0.29 | 14.00 |
| switch_cost_50 | 14.00 | 0.00 | 14.00 |

### 读出来的信号

1. **`baseline` 依然是全场最优**（14.33/15，数字完全没变），三个 v2 功能都没有让默认配置本身
   变差或变好——符合预期，因为 Taunt/克制对照表/延迟收益打分主要在"默认框架原本判断不够好"的
   边缘情况才会介入，默认配置本来就没怎么依赖这些边缘情况。
2. **`setup_weight_15` 从 13.67 回到 14.00，`switch_cost_0` 从 14.00 掉到 13.67**——两个都在
   之前反复确认过的噪声范围内波动（这几个参数的"最优值"本来就对系统其它部分的改动敏感，之前
   已经多次记录过），不代表这两个参数本身有新变化。
3. **`no_opening_script` 单独测时（只测这一组）曾经涨到 13.33，但凑完整 7 组重新出图后又回落到
   13.00**——查过 per-bot 明细，输的还是两个已知老问题（`max_damage-uber` 超时、`simple-uber`
   回归），不是延迟收益打分引入了新问题，纯粹是 3 次重跑这个样本量本身的波动。这本身是个值得记的
   方法论案例：同一个配置换一批重跑，数字会变，不能只看一次结果就下结论（呼应 3.1 节）。
4. **`max_damage-uber` 连续第七轮精确复现同一个数字**（`baseline` 1/3 且 stdev=0.58，其余全部
   0/3），跨越五种完全不同方向的改动都没变过，是目前证据最充分、最终的结论：这是当前决策逻辑
   范式（单回合贪心打分，不做多回合搜索/预判）解决不了的结构性损失，不再继续尝试针对性解决。

### 结论（v2 阶段性收尾，本表已被下方 2026-07-24 的修正表取代）

- v1 默认配置（`ENABLE_HARD_KO=True`/`ENABLE_RIBOMBEE_OPENING=True`/`SETUP_BOOST_WEIGHT=30`/
  `SWITCH_DEFENSE_WEIGHT=60`/`SWITCH_COST=25`）依然是综合表现最好的一版，v2 加的三个新能力
  都不影响这个结论，全部保留在代码里（都是通用机制，没有证据表明有负面影响）。
- ~~`max_damage-uber` 的超时问题正式认定为超出当前算法范式能解决的范围~~——这条结论是错的，
  见下方 2026-07-24 的修正。

## 2026-07-24 `max_damage-uber` 超时结论修正 + 全部配置用修复后工具重新验证

`analysis/run_ablation.py --repeats N` 原来在同一个进程里循环 N 次，为了避免"nametaken"报错，
每个 repeat 用递增的用户名——但代价是旧连接从不会被清理，同一进程生命周期内不断累积，实测会
拖慢本地 Showdown 服务器、虚增超时率（细节见 issue #14）。修好之后（每个 repeat 跑成独立子
进程，复现 `expert_main.py` 真实评测时"每次都是全新进程、用户名固定"的干净行为），把当前
`CONFIGS` 里全部 9 个配置各重跑 3 次：

| 配置 | mean_beaten/15 | stdev | 备注 |
|---|---|---|---|
| **baseline** | **15.00** | 0.00 | 零超时 |
| no_hard_ko | 14.67 | 0.29 | 真实游戏内损失：`simple-uber`/`random-uber` |
| no_opening_script | 14.33 | 0.29 | 真实游戏内损失：`simple-uber` |
| setup_weight_15 | 15.00 | 0.00 | |
| setup_weight_45 | 14.67 | 0.29 | 真实游戏内损失：`simple-uber`/`simple-ou` |
| switch_defense_weight_30 | 15.00 | 0.00 | |
| switch_defense_weight_90 | 15.00 | 0.00 | |
| switch_cost_0 | 15.00 | 0.00 | |
| switch_cost_50 | 15.00 | 0.00 | |

**读出来的信号**：
1. `max_damage-uber` 在全部 9 个配置、27 次独立重跑里**一次都没有出现在"没打满分"的名单里**——
   彻底confirmed 不再是弱点，issue #4 的"结构性、解决不了"结案定论已修正重开。
2. 干净数据下，`baseline`/`setup_weight_15`/`switch_defense_weight_30`/`switch_defense_weight_90`/
   `switch_cost_0`/`switch_cost_50` 六个配置全部是 15.00/0.00——这几个可调常量在测试过的取值范围内
   比之前认为的更不敏感（之前的"14.33 vs 14.00"这类差异，很大一部分就是被超时噪声制造出来的）。
3. 唯二还留有损失的两个配置（`no_hard_ko`/`no_opening_script`，加上被同一个对手拖累的
   `setup_weight_45`）损失的对手清一色是 `simple-uber`——查过 `per_bot_summary.csv`
   确认不是超时（`winrate` 不是精确 0.0），是真实游戏内输赢，跟 `simple-uber` 是 poke_env 自带的
   `SimpleHeuristicsPlayer`（真的会铺钉子/强化/换人，不是 `max_damage` 那种纯贪心）这个已知
   发现吻合，是这两个模块（硬短路 KO、Ribombee 开局脚本）真实的、可解释的贡献。

## 下一步

1. 转向 issue #9/#11（Report Design/Reflections）或探索太晶化（issue #5，已完成，见
   `Experiment_Log.md`）这类全新决策维度。
2. 如果以后队伍加了反射壁/光墙类己方场地招式，延迟收益打分公式已经通用支持，不用再单独写。
3. `simple-uber`（真正有策略的对手）现在是数据上唯一还有真实损失的对手，值得列为下一个
   排查方向。
3. 有需要时可以用修复后的工具重新跑一遍完整 7 配置横向对比，替换上表里仍然过时的数字。

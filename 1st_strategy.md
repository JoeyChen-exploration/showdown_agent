队伍Koraidon，Zacian-Crowned为高速抢攻位 所以主做输出

1. 首发Ribombee, 上场无脑放钉子（减速度），因为特殊道具所以有免死金牌（我自己的理解是），所以不怕OTK。然后如果对面不是草精灵，则直接麻痹粉，如果是草精灵或者被克制，直接U-turn换上Kyogre。这时商场自动唤起雨天，直接瀑布攀登，打一波雨天的120威力的贯伤。

然后以我的游戏经验，如果玩这种速攻队，打OTK时核心。我觉得主要策略就是这些，你可以跟我交流一下然后补充。

---

## 补充设计（跟 Claude 讨论后细化，2026-07-21）

### 前提：对手 teampreview 是随机的

验证了 `poke_env` 源码：`Player`/`SimpleHeuristicsPlayer` 都没覆写 `teampreview`，默认走
`random_teampreview`——**15 个 bot 每局开场用哪只精灵都是随机抽的**，不能假设固定对手。
所以规则必须是运行时根据 `battle.opponent_active_pokemon` 判断的通用逻辑，不能针对"对面大概率开谁"写死分支。
我方 `teampreview` 固定 `/team 1`，单打里只有首发重要（Ribombee），后续换人顺序不受限制。

### 整体决策架构：硬短路规则 + 单回合价值打分

不是纯 if-elif 优先级链，也不做多回合搜索/minimax（对面是不会博弈的固定 bot，没必要），而是：

1. **极少数"结果确定"的场景直接硬短路**，不进打分系统：目前只有一条——**这回合有招式能斩杀对面就直接用**（多个能斩杀的招式里选更安全的，比如没反作用力的优先）。硬短路的意义是让这类确定性结论在代码/报告里能一句话讲清楚，不用绕到"因为分数 0.97 > 0.82"。
2. **其余所有场景（打哪个招式、要不要换人、要不要铺垫）统一走单回合价值打分**，对当前所有合法动作分别算分，选分数最高的。
   - 打分只看**这一回合**的结果，不做多回合推演。
   - 换人评分：从**防御端**（对面招式打在候选换人身上的属性倍率，越低越安全）和**攻击端**（候选人招式打在对面身上的属性倍率，越高越好）两个方向综合打分，谁分高换谁。这个函数是通用的，不专门为 Kyogre 写死，只是 Kyogre 常规情况下大概率算出来分数最高。

### Ribombee 开局分支（固定脚本，不进打分系统）

Ribombee 首发是刻意设计好的固定开局脚本，优先级高于通用打分框架：

```
第 1 回合：无脑放 Sticky Web（黏黏网，减对面换人速度）

第 2 回合：
  对面是草系？（Stun Spore 对草系必定免疫，不浪费回合尝试）
    → 是：直接 U-turn 换 Kyogre
    → 否：查"换人评分"——现在换 Kyogre 划算吗？
          → 划算（被克制）：U-turn 换 Kyogre
          → 不划算：放 Stun Spore（麻痹粉）
```

"被克制"这个词统一复用换人评分函数的结果，不单独定义一套标准，避免开局和中后期用两套不一致的判断。

U-turn 换上 Kyogre 后，唤雨（Drizzle）自动生效，配合雨天加成的瀑布攀登（120 等效威力）是既定的强开局连招。

### Focus Sash 的注意事项

Focus Sash 只在"满血状态下被斩杀"时触发。第一回合场上还没有钉子，这局稳吃保命没问题。
但 Ribombee 是虫/妖精，对岩石系是弱点——**如果后期又把 Ribombee 换回场，且对面已经放过隐形岩，
一上场先掉 25%，Focus Sash 直接作废**。规则上明确"Ribombee 只在开局用一次，不主动换回来"。

### 铺垫招式（剑舞/冥想）的安全触发条件

铺垫本身不产生这回合的伤害，价值来自"以后打得更狠"，所以核心问题是"以后能不能兑现"。
简化公式：`铺垫分数 = 强化本身的价值 × 这回合能扛住对面攻击的把握`。

**关键补充：判断"扛不扛得住"之前，必须先比双方速度，因为谁先手决定了要用哪套数值去算：**

- **我方更快**：铺垫这个动作这回合真的会生效（不会被对面抢先打死导致落空）。
  - 如果铺垫招式本身也加防御向数值（冥想同时加特防、铁壁加防御），"扛不扛得住"可以用**铺垫后**的数值去算对面这波攻击，判断更宽松。
  - 如果铺垫只加攻击向数值（剑舞只加攻击，不加防御），"扛不扛得住"还是要用**铺垫前**的防御数值算，跟没铺垫时一样。
- **对面更快**：对面这回合会先出手，打在我方**还没来得及铺垫**的身上——如果这一下直接把我方打死/打残，铺垫等于完全没兑现就没了（比正常输出还差，因为一点伤害都没造成）。这种情况下"扛不扛得住"必须用**铺垫前（当前）**的数值去算，标准要收紧，不能用"铺垫后应该能扛住"这种事后才成立的状态来判断，因为对面先手时那个状态根本不会出现。

完整链条：**先比速度谁快 → 根据先后手决定用哪套数值判断"扛不扛得住" → 扛得住才给铺垫打分，扛不住铺垫分数直接归零**（此时正常输出/换人的分数会更高，铺垫选项被自然淘汰）。

### 明确延后到 v2：太晶化（Tera）

太晶化需要临场判断"这一下能不能把输的局面掰回来"，涉及变量太多，第一版先不实现。
**不用不会破坏任何东西**——队伍文本里的 Tera Type 字段留着不动，只要 `_choose_move` 不主动触发
太晶化这个动作，所有精灵全程按自己本来的属性正常战斗。

这是一个天然的后续实验点：先跑一版不带太晶的基线，之后单独加上太晶判断逻辑再跑一次对比，
前后差异记进 `Experiment_Log.md`。

### v1 已实现并跑出结果（2026-07-21）

以上设计已经写进 `players/wche652.py` 的 `_choose_move`，跑出 `ranked #3, mark 9.0 (13/15 bots beaten)`——
`max_damage`/`random` 两档 10 局全胜，`simple` 档 3/5（ou、uu 还没赢）。完整改动内容、打分公式当前用的
简化启发式（不是精确伤害计算器）说明、下一步排查方向，见 `Experiment_Log.md` 对应条目。

### 追踪基础设施（2026-07-21 已加上）

`wche652.py` 里加了 `self.decision_log`（每回合记一条：分支、候选动作和分数、最终选择），
配合新增的 `analysis/run_ablation.py`（不在 `players/` 里，不受写盘限制）可以跑消融实验、
把 `decision_log` 和 poke_env 自带的 `battle.observations`（双方每回合完整事件）落盘分析。
冒烟测试第一场就用这套工具挖到一个真实问题：对手速度估算在"认真堆速度"的对手身上会系统性偏低
（`_effective_speed` 对未知对手退化成种族值，忽略了真实努力值投入），细节见 `Experiment_Log.md`
2026-07-21 "加追踪基础设施"条目和 `Report_Outline.md` 的 Reflections 素材部分。

### 待细化（还没聊完，按顺序往下推）

- [x] 排查 `simple-ou`/`simple-uu` 具体输在哪个环节——2026-07-22 用 `decision_log` 定位到，见
      issue #6（连带挖出并修复了 `_switch_score` 一个方向反了的 bug）
- [x] 对手速度估算——2026-07-23 修复：种族值改成假设投满速度个体值/努力值/正性格的保守估算
      （`_max_speed_estimate`），见 issue #3
- [x] `_find_ko_move` 的硬短路判断没看招式威力——2026-07-23 修复：改成跟 `_incoming_threat_score`
      一致口径的 `_power_score`/`INCOMING_THREAT_SCALE` 估算，见 issue #13
- [x] Judgment（Arceus-Ghost 招牌招式）类型没跟着阵型动态解析，永远按一般系算——2026-07-23
      全文件系统性 bug 审查挖到，修复：新增 `_effective_move_type`，对 Judgment 特判用攻击方
      自己的类型（真实游戏里 Judgment 永远跟使用者类型一致）
- [x] Scale Shot（Koraidon 核心招式）多段攻击威力被低估约 3 倍，没乘期望命中次数
      （`move.expected_hits`）——2026-07-23 修复
- [x] `_score_move` 判断钉子招式是否重复，默认都是打对面场地——2026-07-23 修复：改成按
      `move.target` 判断查己方还是对面场地字典（现在队伍里只有黏黏网，暂时没实际影响，
      是给以后加反射壁类招式预防的）
- [ ] （Suggestion，未处理）KO 判断/伤害估算没打命中率折扣，85% 命中的招式被当成 100% 稳中
- [ ] （Suggestion，未处理）通用打分框架没检查状态招式的属性免疫（比如草系免疫麻痹粉），
      开局脚本自己特判了但没复用到通用框架
- [ ] （Suggestion，未处理）Ribombee 开局脚本"决定撤退去 Kyogre"这个理由，跟实际执行换人的
      `_best_switch`（重新对全体候选打分）没有强制绑定
- [ ] 打分公式具体因子和权重（目前思路：伤害占比、承伤占比、异常状态附加值，后续视测试效果再加铺垫折扣因子等）
- [ ] Arceus-Ghost（奶盾/收尾）、Eternatus 在整体框架里的角色定位
- [ ] **v2**：打 `max_damage-uber`（对面带 `Arceus-Fairy`，冥想+生命种子）容易拖到 39-42 回合，
      逼近 `expert_main.py` 90 秒的超时判负线——90 秒是评测规则本身，不能放宽，只能让打法更果断
      来解决，需要专门设计"怎么更快结束拉锯战"（2026-07-21 消融实验发现，详见 `Ablation_Study.md`）

### 已知短板（来自 Opponent_Roster.md 的跨档观察）

- 队伍没人能清钉子/免疫钉子（没人带 Heavy-Duty Boots），频繁换人会持续掉血。
- Kyogre 的 Drizzle 天气会削弱队伍里 Koraidon 的 Flare Blitz（火系招式雨天减半）。